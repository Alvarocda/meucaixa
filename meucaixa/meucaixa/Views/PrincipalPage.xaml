<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="meucaixa.Views.PrincipalPage"
             Visual="Material"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{StaticResource RichBlack}">
    <ContentPage.Content>
        <StackLayout Spacing="10" Padding="5">
            <Button ImageSource="real96x.png" Text="Fechar Caixa" FontSize="30" BackgroundColor="{StaticResource Verde2}" Command="{Binding AbreFechamentoCaixaCommand}" VerticalOptions="FillAndExpand" ContentLayout="Top,10"/>
            <Button ImageSource="chart96px.png" Text="Histórico" FontSize="30" BackgroundColor="{StaticResource Carolina}" Command="{Binding AbreHistoricoCommand}" VerticalOptions="FillAndExpand" ContentLayout="Top,5"/>
            <Button ImageSource="sair96px.png" Text="Sair" FontSize="30" BackgroundColor="{StaticResource RadicalRed}" Command="{Binding SairCommand}" VerticalOptions="FillAndExpand" ContentLayout="Top,1"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>